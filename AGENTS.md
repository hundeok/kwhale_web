# K-Whale 작업 행동강령

이 파일은 사람과 AI를 포함해 이 저장소에서 작업하는 모든 기여자가 가장 먼저
읽어야 하는 운영 규칙이다. 제품의 맥락이 사라져도 같은 품질 기준으로 작업을
이어가기 위한 최상위 지침이다.

## 1. 프로젝트 경계

- 본진은 이 디렉터리 `kwhale_web_gpt`다.
- `kwhale_web`은 복구된 과거 원본이며 절대 수정하지 않는다.
- 사용자가 명시적으로 요청하지 않는 한 본진을 다른 프로젝트에 덮어쓰거나
  역으로 원본을 본진에 덮어쓰지 않는다.
- `backend/private-data`, SQLite 릴리스, 원본 파일과 lineage는 공개 정적
  디렉터리로 복사하지 않는다.
- 작업 중 발견한 사용자 변경사항은 보존한다. 관련 없는 파일을 되돌리지 않는다.

## 2. 제품 정체성

K-Whale은 공식 공직자 재산공개 기록을 검증 가능한 데이터 계보와 함께
탐색하는 Public Asset Intelligence 제품이다.

우선순위는 다음과 같다.

1. 데이터 정합성
2. 의미의 정확성
3. 재현성과 계보
4. 화면 안정성
5. 탐색 속도와 가독성
6. 시각적 완성도

멋진 화면이 정확한 숫자보다 앞설 수 없다. 추정값을 공식값처럼 보이게 만들지
않으며, 설명할 수 없는 통계는 출시하지 않는다.

## 3. 데이터 불변 규칙

- 공식 신고총액과 종목별 직접 귀속 금액을 구분한다.
- 복수 종목 합산 신고액을 종목별로 임의 배분하지 않는다.
- 채무는 총자산에서 차감한다.
- 연도 비교는 동일 인물·동일 자산 매칭 근거가 있을 때만 수행한다.
- `recent`, 개별 연도, `all`의 의미를 혼용하지 않는다.
- 원문 명칭과 정규화 명칭을 모두 보존한다.
- 숫자에는 단위와 모집단을 표시한다.
- 파생 통계는 공식 신고값과 시세·수익률 예측이 아님을 명시한다.
- 지도 좌표는 상세 주소 좌표와 행정·대표 좌표를 구분한다.
- 서로 다른 상세 주소가 동일 좌표를 공유하면 정밀 좌표로 표시하지 않는다.

상세 규칙은 `docs/data-methodology.md`와 `docs/data-governance.md`를 따른다.

## 4. UI/UX 원칙

- 기본 테마는 다크 모드다.
- 라이트 모드는 단순 색상 반전이 아니라 별도 가독성 검수를 거친다.
- 너무 작은 보조문구를 남발하지 않는다.
- 이름·기관·지역·자산 등 고유 요소는 장기적으로 고유 상세 페이지와 연결할 수
  있는 식별자를 유지한다.
- 필터와 렌즈는 가능한 경우 다시 클릭하면 해제되는 토글로 동작한다.
- 기본 화면은 최신 통합 스냅샷을 사용하되, 연도와 ALL-TIME 의미를 명확히 한다.
- 숫자는 `toLocaleString('ko-KR')` 또는 공통 포매터를 사용해 천 단위 구분을
  일관되게 적용한다.
- 기능이 없는 장식, 더미 통계, 임의 랜덤값을 추가하지 않는다.

## 5. 지도 전용 안전 규칙

지도는 과거 그래픽 작업 중 좌표 앵커와 CSS `transform`이 충돌한 이력이 있다.

- MapLibre가 소유한 마커 좌표 컨테이너의 `transform`을 CSS로 수정하거나
  transition하지 않는다.
- 베이스 타일, `tileSize`, 위경도, Supercluster 계산을 그래픽 작업과 함께
  변경하지 않는다.
- 애니메이션과 소나 그래픽은 좌표 컨테이너 내부 자식 요소에서만 실행한다.
- 이전 마커와 새 마커를 좌표 위에 중복 보존하는 교대 렌더링을 사용하지 않는다.
- 지도 변경 후 전국 기본 마커 수, 연속 확대, 필터 초기화, 개별 자산 위치를
  반드시 회귀 검증한다.
- 대청도·백령도처럼 본토에서 멀리 떨어진 도서 좌표를 임의로 본토로 이동하지
  않는다.

세부 사고 기록은 `docs/decision-log.md`를 참고한다.

## 6. 표준 작업 흐름

1. 요청과 영향 범위를 확인한다.
2. 실제 코드·데이터를 읽고 가설을 세운다.
3. 데이터 계산 변경과 시각 변경을 분리한다.
4. 가장 작은 안전 단위로 구현한다.
5. 정적 검사와 빌드를 수행한다.
6. 관련 API와 실제 브라우저 화면을 검증한다.
7. 숫자·URL 상태·다크/라이트·오류 경로를 회귀 확인한다.
8. 결과와 남은 위험을 문서에 기록한다.

대규모 변경은 한 번에 여러 메뉴에 확산하지 않는다. 대표 메뉴에서 검증한 뒤
공통 컴포넌트로 확장한다.

## 7. 필수 검증 명령

프런트엔드:

```bash
npm run build
```

백엔드:

```bash
cd backend
node --check server_private.js
npm test
python3 -m unittest discover -s test -p 'test_*.py'
```

데이터 릴리스:

```bash
cd backend
npm run data:audit-dataset
npm run data:semantic-quality
```

데이터 파이프라인을 변경하지 않은 UI 작업은 전체 파이프라인 재생성을 요구하지
않지만, 해당 API 응답과 화면 값은 검증해야 한다.

## 8. 로컬 실행 기준

프런트엔드:

```bash
npm run dev
```

API:

```bash
cd backend
npm run api:private
```

- 프런트엔드: `http://127.0.0.1:5173`
- 비공개 API: `http://127.0.0.1:3340`
- Vite의 `/api` 프록시는 3340을 향한다.
- `server.js`/3333은 레거시 경로다. 현재 제품 검증은 `server_private.js`를
  기준으로 한다.

## 9. 완료 정의

작업은 코드 작성만으로 완료되지 않는다.

- 요청한 동작이 실제 화면에서 작동한다.
- 데이터 의미와 모집단이 맞다.
- 빌드가 통과한다.
- 기존 핵심 경로가 깨지지 않는다.
- 오류·빈 데이터·작은 표본이 정직하게 표시된다.
- 필요한 문서가 갱신된다.

## 10. 문서 지도

- `docs/project-handbook.md`: 제품 전체 구조와 메뉴 현황
- `docs/data-methodology.md`: 계산·분류 방법론
- `docs/data-platform.md`: 원본·릴리스·파이프라인 구조
- `docs/data-governance.md`: 데이터 신뢰도와 공개 정책
- `docs/quality-runbook.md`: 개발·검증·복구 절차
- `docs/decision-log.md`: 주요 설계 결정과 사고 교훈
- `docs/roadmap.md`: 다음 단계와 장기 목표
- `docs/current-state.md`: 현재 개발 지점과 바로 다음 작업
